config {
    type: "table",
    schema: "wiki_push",
    bigquery: {
        partitionBy: "timestamp"
    }
}

select user, title, timestamp, current_timestamp() as processed_time
from wiki_push.wiki_changes